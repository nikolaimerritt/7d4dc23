@model CTFWhodunnit.Models.Suspect

@{
    ViewData["Title"] = "Success";
}

<div class="container">

    <div class="alert alert-success text-center">
        <h2>Well done!</h2>
        <p>You have identified the mastermind!</p>
        <h5 class="card-title">@Model.Name</h5>
        <img class="w-300" src="@("/imgs/avatars/" + @Model.SuspectId + ".jpeg")" alt="Card image cap">
    </div>


</div>